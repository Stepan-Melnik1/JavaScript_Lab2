# Лабораторная работа №2 — Базовые методы работы с массивами

## Цель работы

Реализовать базовые методы работы с массивами (`forEach`, `map`, `filter`, `find`, `some`, `every`, `reduce`) **вручную**, без использования встроенных методов JavaScript. Только цикл `for`, свойство `length`, обращение по индексу и метод `push`.

---

## Содержание

1. [printArray / printArray1](#1-printarray--printarray1)
2. [forEach](#2-foreach)
3. [map](#3-map)
4. [filter](#4-filter)
5. [find](#5-find)
6. [some](#6-some)
7. [every](#7-every)
8. [reduce](#8-reduce)
9. [Контрольные вопросы](#контрольные-вопросы)

---

## 1. printArray / printArray1

### Что делает?

Две функции для вывода элементов массива в консоль — каждая в своём формате.

### Зачем два варианта?

Показывает проблему без колбэков: для каждого формата вывода приходится писать **отдельную функцию**, дублируя логику цикла. Решение этой проблемы — функция `forEach` с колбэком (см. раздел 2).

### Код

```javascript
/**
 * Выводит элементы массива в формате "Element N: value X"
 * @param {Array} array - массив для вывода
 */
function printArray(array) {
  if (!Array.isArray(array)) {
    throw new TypeError("Аргумент должен быть массивом");
  }
  for (let i = 0; i < array.length; i++) {
    console.log(`Element ${i}: value ${array[i]}`);
  }
}

/**
 * Выводит элементы массива в формате "N: X"
 * @param {Array} array - массив для вывода
 */
function printArray1(array) {
  if (!Array.isArray(array)) {
    throw new TypeError("Аргумент должен быть массивом");
  }
  for (let i = 0; i < array.length; i++) {
    console.log(`${i}:  ${array[i]}`);
  }
}
```

### Пример использования

```javascript
printArray(["яблоко", "банан", "вишня"]);
// Element 0: value яблоко
// Element 1: value банан
// Element 2: value вишня

printArray1(["яблоко", "банан", "вишня"]);
// 0:  яблоко
// 1:  банан
// 2:  вишня
```

### Как работает? Построчный разбор

```javascript
function printArray(array) {
```
Объявляем функцию с именем `printArray`. В скобках — параметр `array`, сюда придёт массив когда кто-то вызовет функцию.

```javascript
  if (!Array.isArray(array)) {
    throw new TypeError("Аргумент должен быть массивом");
  }
```
**Защита.** `Array.isArray(array)` проверяет — массив ли это. `!` переворачивает результат: если это НЕ массив — входим в блок `if` и выбрасываем ошибку. Программа останавливается с понятным сообщением.

```javascript
  for (let i = 0; i < array.length; i++) {
```
Цикл `for` состоит из трёх частей:
- `let i = 0` — создаём счётчик `i`, ставим в 0 (индексы в JS начинаются с нуля)
- `i < array.length` — крутимся пока `i` меньше длины массива. Для `["яблоко","банан","вишня"]` длина = 3, значит `i` пройдёт 0, 1, 2 — это ровно все индексы
- `i++` — после каждого шага увеличиваем `i` на 1

```javascript
    console.log(`Element ${i}: value ${array[i]}`);
```
Выводим строку. Обратные кавычки `` ` `` позволяют вставлять переменные через `${}`. `array[i]` — обращение к элементу по индексу: когда `i=0` это `array[0]`, когда `i=1` это `array[1]` и так далее.

```
Пошагово для ["яблоко", "банан", "вишня"]:

  Старт: i = 0
  ┌─────────────────────────────────────────────────────┐
  │ i=0 │ 0 < 3 = true  │ array[0]="яблоко" │ выводим  │
  │ i=1 │ 1 < 3 = true  │ array[1]="банан"  │ выводим  │
  │ i=2 │ 2 < 3 = true  │ array[2]="вишня"  │ выводим  │
  │ i=3 │ 3 < 3 = false │ цикл заканчивается│          │
  └─────────────────────────────────────────────────────┘

  Вывод:
  Element 0: value яблоко
  Element 1: value банан
  Element 2: value вишня
```

---

## 2. forEach

### Что делает?

Выполняет переданный колбэк для каждого элемента массива. **Ничего не возвращает** (`undefined`).

### Зачем нужна?

Решает проблему из раздела 1: вместо двух отдельных функций — одна универсальная. Логика вывода задаётся через колбэк:

```javascript
// Один forEach — два разных формата вывода
forEach(arr, (el, i) => console.log(`Element ${i}: value ${el}`));
forEach(arr, (el, i) => console.log(`${i}:  ${el}`));
```

### Код

```javascript
/**
 * Выполняет колбэк для каждого элемента массива.
 * @param {Array} array - исходный массив
 * @param {Function} callback - функция вида (element, index, array) => void
 * @returns {undefined}
 */
function forEach(array, callback) {
  if (!Array.isArray(array)) {
    throw new TypeError("Первый аргумент должен быть массивом");
  }
  if (typeof callback !== "function") {
    throw new TypeError("Второй аргумент должен быть функцией");
  }

  for (let i = 0; i < array.length; i++) {
    callback(array[i], i, array);
  }
}
```

### Пример использования

```javascript
forEach([1, 2, 3], (element, index) => {
  console.log(`Element: ${element}, Index: ${index}`);
});
// Element: 1, Index: 0
// Element: 2, Index: 1
// Element: 3, Index: 2
```

### Как работает? Построчный разбор

```javascript
function forEach(array, callback) {
```
Функция принимает два параметра: `array` — массив для обхода, `callback` — функция которую запустим для каждого элемента.

```javascript
  if (!Array.isArray(array)) {
    throw new TypeError("Первый аргумент должен быть массивом");
  }
```
Проверка первого аргумента. Если передали строку, число или что угодно кроме массива — сразу ошибка.

```javascript
  if (typeof callback !== "function") {
    throw new TypeError("Второй аргумент должен быть функцией");
  }
```
Проверка второго аргумента. `typeof` возвращает тип переменной в виде строки. Если тип не равен `"function"` — ошибка. Это защита от случаев когда случайно передают строку или число вместо функции.

```javascript
  for (let i = 0; i < array.length; i++) {
    callback(array[i], i, array);
  }
```
Сердце функции. Проходим по каждому элементу и вызываем `callback`, передавая ему три вещи: текущий элемент `array[i]`, его индекс `i`, и весь массив `array`. Колбэк делает с этим что хочет — мы не знаем и нам не важно.

```javascript
  // forEach ничего не возвращает
```
В конце нет `return` — функция молча завершается и возвращает `undefined`. Это специально: цель `forEach` — выполнить действие, а не создать что-то новое.

```
Пошагово для forEach([10, 20, 30], (el, i) => console.log(el, i)):

  Проверки пройдены ✅

  ┌──────┬──────────┬───────────────────────────────────────────┐
  │  i   │ array[i] │ что делает колбэк                         │
  ├──────┼──────────┼───────────────────────────────────────────┤
  │  0   │    10    │ console.log(10, 0) → выводит "10 0"       │
  │  1   │    20    │ console.log(20, 1) → выводит "20 1"       │
  │  2   │    30    │ console.log(30, 2) → выводит "30 2"       │
  │  3   │    —     │ 3 < 3 = false → цикл заканчивается        │
  └──────┴──────────┴───────────────────────────────────────────┘

  return undefined
```

### Аргументы колбэка

| Аргумент | Что это | Обязателен? |
|----------|---------|-------------|
| `element` | Текущий элемент массива | ✅ Да |
| `index` | Индекс текущего элемента | ❌ Нет |
| `array` | Весь массив целиком | ❌ Нет |

---

## 3. map

### Что делает?

Создаёт **новый массив**, применяя колбэк к каждому элементу. Длина результата **всегда равна** длине исходного массива.

### Чем отличается от forEach?

```
forEach → просто делает что-то → возвращает undefined
map     → собирает результаты  → возвращает новый массив
```

### Код

```javascript
/**
 * Создаёт новый массив из результатов вызова колбэка для каждого элемента.
 * @param {Array} array - исходный массив
 * @param {Function} callback - функция вида (element, index, array) => newValue
 * @returns {Array} новый массив той же длины
 */
function map(array, callback) {
  if (!Array.isArray(array)) {
    throw new TypeError("Первый аргумент должен быть массивом");
  }
  if (typeof callback !== "function") {
    throw new TypeError("Второй аргумент должен быть функцией");
  }

  const result = [];

  for (let i = 0; i < array.length; i++) {
    result.push(callback(array[i], i, array));
  }

  return result;
}
```

### Пример использования

```javascript
const numbers = [1, 2, 3];
const squared = map(numbers, (el) => el * el);
console.log(squared); // [1, 4, 9]
console.log(numbers); // [1, 2, 3] ← исходный массив не изменился!
```

### Как работает? Построчный разбор

```javascript
function map(array, callback) {
  if (!Array.isArray(array)) { ... }
  if (typeof callback !== "function") { ... }
```
Те же проверки что и в `forEach` — защищаемся от неправильных аргументов.

```javascript
  const result = [];
```
Создаём **пустую корзину**. Сюда будем складывать преобразованные элементы. В `forEach` такой строки не было — это главное отличие.

```javascript
  for (let i = 0; i < array.length; i++) {
    result.push(callback(array[i], i, array));
  }
```
Для каждого элемента:
1. Вызываем `callback(array[i], i, array)` — колбэк что-то делает с элементом и **возвращает новое значение**
2. `result.push(...)` — кладём это новое значение в конец корзины
3. После цикла в `result` столько же элементов сколько в исходном массиве

```javascript
  return result;
```
Возвращаем заполненную корзину. Без этой строки вся работа была бы напрасной — результат бы потерялся.

```
Пошагово для map([1, 2, 3], (el) => el * 10):

  result = []  ← пустая корзина

  ┌──────┬──────────┬─────────────────────┬──────────────────┐
  │  i   │ array[i] │ колбэк возвращает   │ result после     │
  ├──────┼──────────┼─────────────────────┼──────────────────┤
  │  0   │    1     │ 1 * 10 = 10         │ [10]             │
  │  1   │    2     │ 2 * 10 = 20         │ [10, 20]         │
  │  2   │    3     │ 3 * 10 = 30         │ [10, 20, 30]     │
  │  3   │    —     │ 3 < 3 = false, стоп │                  │
  └──────┴──────────┴─────────────────────┴──────────────────┘

  return [10, 20, 30]

  Исходный массив [1, 2, 3] — не изменился!
```

### Ключевое отличие от filter

```javascript
// map — кладёт РЕЗУЛЬТАТ колбэка
result.push(callback(array[i], i, array)); // результат преобразования

// filter — кладёт ОРИГИНАЛЬНЫЙ элемент (только если колбэк = true)
if (callback(array[i], i, array)) {
  result.push(array[i]); // сам элемент без изменений
}
```

---

## 4. filter

### Что делает?

Создаёт **новый массив** только из тех элементов, для которых колбэк вернул `true`. Длина результата **меньше или равна** длине исходного.

### Аналогия

Охранник на входе: пропускает только тех, кто прошёл проверку.

```
Все:      [1, 2, 3, 4, 5]
Проверка:  ✗  ✓  ✗  ✓  ✗  ← колбэк (чётное?)
Прошли:       [2,    4]
```

### Код

```javascript
/**
 * Возвращает новый массив из элементов, прошедших проверку колбэком.
 * @param {Array} array - исходный массив
 * @param {Function} callback - функция вида (element, index, array) => boolean
 * @returns {Array} новый массив (может быть короче исходного)
 */
function filter(array, callback) {
  if (!Array.isArray(array)) {
    throw new TypeError("Первый аргумент должен быть массивом");
  }
  if (typeof callback !== "function") {
    throw new TypeError("Второй аргумент должен быть функцией");
  }

  const result = [];

  for (let i = 0; i < array.length; i++) {
    if (callback(array[i], i, array)) {
      result.push(array[i]);
    }
  }

  return result;
}
```

### Пример использования

```javascript
const numbers = [1, 2, 3, 4, 5];
const evenNumbers = filter(numbers, (el) => el % 2 === 0);
console.log(evenNumbers); // [2, 4]

const words = ["кот", "слон", "як", "тигр"];
const longWords = filter(words, (el) => el.length > 3);
console.log(longWords); // ["слон", "тигр"]
```

### Как работает? Построчный разбор

```javascript
function filter(array, callback) {
  if (!Array.isArray(array)) { ... }
  if (typeof callback !== "function") { ... }
```
Стандартные проверки аргументов.

```javascript
  const result = [];
```
Пустая корзина для элементов которые пройдут проверку.

```javascript
  for (let i = 0; i < array.length; i++) {
    if (callback(array[i], i, array)) {
```
Для каждого элемента вызываем колбэк. Колбэк в `filter` — это **судья**: он должен вернуть `true` (пропустить) или `false` (отклонить). Если вернул `true` — заходим в блок `if`.

```javascript
      result.push(array[i]);
```
Кладём в корзину **оригинальный элемент** `array[i]`, а НЕ результат колбэка. Колбэк вернул `true` или `false` — нам это не нужно в результате. Нам нужен сам элемент.

```javascript
    }
  }
  return result;
```
После цикла возвращаем корзину с отобранными элементами. Она может быть пустой (никто не прошёл), частично заполненной, или такой же длины как исходный массив (все прошли).

```
Пошагово для filter([1, 2, 3, 4, 5], (el) => el % 2 === 0):

  result = []  ← пустая корзина

  ┌──────┬──────────┬──────────────────┬───────────┬──────────────────┐
  │  i   │ array[i] │ колбэк (чётное?) │ берём?    │ result после     │
  ├──────┼──────────┼──────────────────┼───────────┼──────────────────┤
  │  0   │    1     │ 1%2=1 → false    │ ❌ нет    │ []               │
  │  1   │    2     │ 2%2=0 → true     │ ✅ да     │ [2]              │
  │  2   │    3     │ 3%2=1 → false    │ ❌ нет    │ [2]              │
  │  3   │    4     │ 4%2=0 → true     │ ✅ да     │ [2, 4]           │
  │  4   │    5     │ 5%2=1 → false    │ ❌ нет    │ [2, 4]           │
  │  5   │    —     │ 5 < 5 = false    │ стоп      │                  │
  └──────┴──────────┴──────────────────┴───────────┴──────────────────┘

  return [2, 4]

  Было 5 элементов → осталось 2
```

---

## 5. find

### Что делает?

Возвращает **первый элемент**, для которого колбэк вернул `true`. Если такого нет — возвращает `undefined`. После первого совпадения **сразу останавливается**.

### Чем отличается от filter?

```
filter → проходит ВЕСЬ массив → возвращает ВСЕ подходящие элементы → []
find   → останавливается на ПЕРВОМ совпадении → возвращает ОДИН элемент
```

### Код

```javascript
/**
 * Находит первый элемент, для которого колбэк возвращает true.
 * @param {Array} array - исходный массив
 * @param {Function} callback - функция вида (element, index, array) => boolean
 * @returns {*} первый подходящий элемент или undefined
 */
function find(array, callback) {
  if (!Array.isArray(array)) {
    throw new TypeError("Первый аргумент должен быть массивом");
  }
  if (typeof callback !== "function") {
    throw new TypeError("Второй аргумент должен быть функцией");
  }

  for (let i = 0; i < array.length; i++) {
    if (callback(array[i], i, array)) {
      return array[i]; // нашли — сразу возвращаем, цикл прекращается
    }
  }

  return undefined; // ничего не нашли
}
```

### Пример использования

```javascript
const numbers = [1, 2, 3, 4, 5];
const firstEven = find(numbers, (el) => el % 2 === 0);
console.log(firstEven); // 2 ← только первое совпадение, не [2, 4]!

const notFound = find(numbers, (el) => el > 100);
console.log(notFound); // undefined
```

### Как работает? Построчный разбор

```javascript
function find(array, callback) {
  if (!Array.isArray(array)) { ... }
  if (typeof callback !== "function") { ... }
```
Стандартные проверки.

```javascript
  for (let i = 0; i < array.length; i++) {
    if (callback(array[i], i, array)) {
```
Так же как в `filter` — спрашиваем у колбэка про каждый элемент.

```javascript
      return array[i];
    }
  }
```
Вот главное отличие от `filter`: как только колбэк сказал `true` — **сразу возвращаем элемент и выходим из функции**. Слово `return` внутри цикла моментально прерывает и цикл, и всю функцию. Остальные элементы даже не проверяются.

```javascript
  return undefined;
```
Если прошли весь массив и ни разу не зашли в `if` — значит подходящего элемента нет. Возвращаем `undefined`.

```
Пошагово для find([1, 3, 4, 6, 8], (el) => el % 2 === 0):

  ┌──────┬──────────┬──────────────────┬────────────────────────────┐
  │  i   │ array[i] │ колбэк (чётное?) │ действие                   │
  ├──────┼──────────┼──────────────────┼────────────────────────────┤
  │  0   │    1     │ 1%2=1 → false    │ идём дальше                │
  │  1   │    3     │ 3%2=1 → false    │ идём дальше                │
  │  2   │    4     │ 4%2=0 → true     │ return 4 ← ВЫХОД!          │
  │      │          │                  │ элементы 6 и 8 не проверяются│
  └──────┴──────────┴──────────────────┴────────────────────────────┘

  return 4

────────────────────────────────────────────

Пошагово для find([1, 3, 5], (el) => el % 2 === 0):

  ┌──────┬──────────┬──────────────────┬──────────────┐
  │  i   │ array[i] │ колбэк (чётное?) │ действие     │
  ├──────┼──────────┼──────────────────┼──────────────┤
  │  0   │    1     │ false            │ идём дальше  │
  │  1   │    3     │ false            │ идём дальше  │
  │  2   │    5     │ false            │ идём дальше  │
  │  3   │    —     │ 3 < 3 = false    │ цикл кончился│
  └──────┴──────────┴──────────────────┴──────────────┘

  return undefined  ← ничего не нашли
```

---

## 6. some

### Что делает?

Проверяет, есть ли **хотя бы один** элемент, для которого колбэк вернул `true`. Возвращает `true` или `false`. При первом совпадении **сразу останавливается**.

### Аналогия

Вопрос: *"Есть ли в группе хоть один отличник?"* — как только нашли одного, ответ уже `true`, остальных проверять не нужно.

### Код

```javascript
/**
 * Проверяет, удовлетворяет ли хотя бы один элемент условию колбэка.
 * @param {Array} array - исходный массив
 * @param {Function} callback - функция вида (element, index, array) => boolean
 * @returns {boolean} true если нашёлся хоть один подходящий элемент
 */
function some(array, callback) {
  if (!Array.isArray(array)) {
    throw new TypeError("Первый аргумент должен быть массивом");
  }
  if (typeof callback !== "function") {
    throw new TypeError("Второй аргумент должен быть функцией");
  }

  for (let i = 0; i < array.length; i++) {
    if (callback(array[i], i, array)) {
      return true; // нашли хоть один — достаточно!
    }
  }

  return false; // ни один не подошёл
}
```

### Пример использования

```javascript
const numbers = [1, 3, 5, 4, 7];
console.log(some(numbers, (el) => el % 2 === 0)); // true (есть 4)
console.log(some(numbers, (el) => el > 100));     // false (таких нет)
```

### Как работает? Построчный разбор

```javascript
function some(array, callback) {
  if (!Array.isArray(array)) { ... }
  if (typeof callback !== "function") { ... }
```
Стандартные проверки.

```javascript
  for (let i = 0; i < array.length; i++) {
    if (callback(array[i], i, array)) {
      return true;
    }
  }
```
Проходим по массиву. Как только колбэк вернул `true` для любого элемента — **сразу возвращаем `true` и выходим**. Смысл: нам не нужно знать ВСЕ подходящие элементы — достаточно найти ОДИН.

```javascript
  return false;
```
Этой строки достигаем только если прошли весь массив и ни разу не зашли в `if`. Значит ни один элемент не подошёл — возвращаем `false`.

```
Пошагово для some([1, 3, 5, 4, 7], (el) => el % 2 === 0):

  ┌──────┬──────────┬──────────────────┬────────────────────────────┐
  │  i   │ array[i] │ колбэк (чётное?) │ действие                   │
  ├──────┼──────────┼──────────────────┼────────────────────────────┤
  │  0   │    1     │ false            │ идём дальше                │
  │  1   │    3     │ false            │ идём дальше                │
  │  2   │    5     │ false            │ идём дальше                │
  │  3   │    4     │ true             │ return true ← ВЫХОД!       │
  │      │          │                  │ элемент 7 не проверяется   │
  └──────┴──────────┴──────────────────┴────────────────────────────┘

  return true

────────────────────────────────────────────

Пошагово для some([1, 3, 5], (el) => el % 2 === 0):

  ┌──────┬──────────┬──────────────────┬──────────────┐
  │  i   │ array[i] │ колбэк           │ действие     │
  ├──────┼──────────┼──────────────────┼──────────────┤
  │  0   │    1     │ false            │ идём дальше  │
  │  1   │    3     │ false            │ идём дальше  │
  │  2   │    5     │ false            │ идём дальше  │
  │  3   │    —     │ 3 < 3 = false    │ цикл кончился│
  └──────┴──────────┴──────────────────┴──────────────┘

  return false  ← ни одного чётного не нашли
```

---

## 7. every

### Что делает?

Проверяет, **все ли элементы** удовлетворяют условию. Возвращает `true` только если колбэк вернул `true` для **каждого** элемента. При первом несоответствии **сразу останавливается**.

### Сравнение с some

```
some  — ищет true,  останавливается на первом true
every — ищет false, останавливается на первом false
```

### Код

```javascript
/**
 * Проверяет, удовлетворяют ли все элементы условию колбэка.
 * @param {Array} array - исходный массив
 * @param {Function} callback - функция вида (element, index, array) => boolean
 * @returns {boolean} true если все элементы подходят, иначе false
 */
function every(array, callback) {
  if (!Array.isArray(array)) {
    throw new TypeError("Первый аргумент должен быть массивом");
  }
  if (typeof callback !== "function") {
    throw new TypeError("Второй аргумент должен быть функцией");
  }

  for (let i = 0; i < array.length; i++) {
    if (!callback(array[i], i, array)) {
      return false; // нашли несоответствие — дальше проверять смысла нет
    }
  }

  return true; // все элементы прошли проверку
}
```

### Пример использования

```javascript
console.log(every([2, 4, 6], (el) => el % 2 === 0)); // true (все чётные)
console.log(every([2, 3, 6], (el) => el % 2 === 0)); // false (3 нечётное)
```

### Как работает? Построчный разбор

```javascript
function every(array, callback) {
  if (!Array.isArray(array)) { ... }
  if (typeof callback !== "function") { ... }
```
Стандартные проверки.

```javascript
  for (let i = 0; i < array.length; i++) {
    if (!callback(array[i], i, array)) {
      return false;
    }
  }
```
Вот хитрость: мы проверяем **противоположное** условие через `!`. Логика такая: *"Если хоть один элемент НЕ подходит — сразу возвращаем `false` и выходим"*. Это зеркало `some`: `some` ищет первый `true`, `every` ищет первый `false`.

```javascript
  return true;
```
Если дошли сюда — значит ни разу не нашли элемент который НЕ подходит. То есть все подошли. Возвращаем `true`.

```
Пошагово для every([2, 4, 6], (el) => el % 2 === 0):

  ┌──────┬──────────┬──────────────────┬──────────────────────────┐
  │  i   │ array[i] │ колбэк (чётное?) │ действие                 │
  ├──────┼──────────┼──────────────────┼──────────────────────────┤
  │  0   │    2     │ true → !true=false│ false в if → идём дальше│
  │  1   │    4     │ true → !true=false│ false в if → идём дальше│
  │  2   │    6     │ true → !true=false│ false в if → идём дальше│
  │  3   │    —     │ 3 < 3 = false     │ цикл кончился           │
  └──────┴──────────┴──────────────────┴──────────────────────────┘

  return true ← все прошли!

────────────────────────────────────────────

Пошагово для every([2, 3, 6], (el) => el % 2 === 0):

  ┌──────┬──────────┬───────────────────┬────────────────────────────┐
  │  i   │ array[i] │ колбэк (чётное?)  │ действие                   │
  ├──────┼──────────┼───────────────────┼────────────────────────────┤
  │  0   │    2     │ true → !true=false │ false в if → идём дальше  │
  │  1   │    3     │ false→ !false=true │ true в if → return false! │
  │      │          │                   │ элемент 6 не проверяется   │
  └──────┴──────────┴───────────────────┴────────────────────────────┘

  return false ← нашли несоответствие, дальше проверять смысла нет
```

---

## 8. reduce

### Что делает?

Проходит по массиву и **накапливает результат** в переменной-аккумуляторе. Возвращает итоговое значение аккумулятора.

### Аналогия

Копилка: кидаешь монеты одну за другой, каждый раз сумма увеличивается. В конце достаёшь итог.

```
[1, 2, 3, 4, 5]  начальное значение: 0

  acc=0  + el=1 → acc=1
  acc=1  + el=2 → acc=3
  acc=3  + el=3 → acc=6
  acc=6  + el=4 → acc=10
  acc=10 + el=5 → acc=15

  return 15
```

### Код

```javascript
/**
 * Последовательно обрабатывает элементы массива, накапливая результат.
 * @param {Array} array - исходный массив
 * @param {Function} callback - функция вида (accumulator, element, index, array) => newAccumulator
 * @param {*} [initialValue] - начальное значение аккумулятора (необязательно)
 * @returns {*} итоговое значение аккумулятора
 */
function reduce(array, callback, initialValue) {
  if (!Array.isArray(array)) {
    throw new TypeError("Первый аргумент должен быть массивом");
  }
  if (typeof callback !== "function") {
    throw new TypeError("Второй аргумент должен быть функцией");
  }

  const hasInitialValue = arguments.length >= 3;

  // Граничный случай: пустой массив без начального значения
  if (array.length === 0 && !hasInitialValue) {
    return undefined;
  }

  let accumulator;
  let startIndex;

  if (hasInitialValue) {
    accumulator = initialValue; // стартуем с переданного значения
    startIndex = 0;             // обходим с первого элемента
  } else {
    accumulator = array[0];     // первый элемент становится стартовым значением
    startIndex = 1;             // обходим со второго элемента
  }

  for (let i = startIndex; i < array.length; i++) {
    accumulator = callback(accumulator, array[i], i, array);
  }

  return accumulator;
}
```

### Пример использования

```javascript
const numbers = [1, 2, 3, 4, 5];

// Сумма с начальным значением
const sum = reduce(numbers, (acc, el) => acc + el, 0);
console.log(sum); // 15

// Сумма без начального значения (первый элемент = аккумулятор)
const sum2 = reduce(numbers, (acc, el) => acc + el);
console.log(sum2); // 15

// Максимальное число
const max = reduce(numbers, (acc, el) => el > acc ? el : acc);
console.log(max); // 5

// Пустой массив без начального значения
console.log(reduce([], (acc, el) => acc + el)); // undefined
```

### Как работает? Построчный разбор

```javascript
function reduce(array, callback, initialValue) {
```
Три параметра: массив, колбэк, и необязательное начальное значение аккумулятора.

```javascript
  const hasInitialValue = arguments.length >= 3;
```
`arguments` — это специальный объект в JS, который содержит все переданные аргументы. `arguments.length` — их количество. Это нужно чтобы отличить `reduce(arr, cb, 0)` (передали `0`) от `reduce(arr, cb)` (ничего не передали). Нельзя просто проверить `initialValue === undefined`, потому что пользователь мог специально передать `undefined`.

```javascript
  if (array.length === 0 && !hasInitialValue) {
    return undefined;
  }
```
Граничный случай: пустой массив и нет начального значения — непонятно что возвращать, возвращаем `undefined`.

```javascript
  let accumulator;
  let startIndex;

  if (hasInitialValue) {
    accumulator = initialValue;
    startIndex = 0;
  } else {
    accumulator = array[0];
    startIndex = 1;
  }
```
Два сценария старта:
- **Есть `initialValue`**: аккумулятор = переданное значение, начинаем с первого элемента (индекс 0)
- **Нет `initialValue`**: аккумулятор = первый элемент массива, начинаем со второго (индекс 1) — иначе обработаем первый элемент дважды

```javascript
  for (let i = startIndex; i < array.length; i++) {
    accumulator = callback(accumulator, array[i], i, array);
  }
```
На каждом шаге: вызываем колбэк с текущим аккумулятором и элементом, колбэк возвращает **новое значение аккумулятора** — перезаписываем его. Это и есть "накопление".

```javascript
  return accumulator;
```
После цикла в `accumulator` финальный результат всех накоплений.

```
Пошагово для reduce([1, 2, 3, 4, 5], (acc, el) => acc + el, 0):

  hasInitialValue = true
  accumulator = 0  ← стартовое значение
  startIndex = 0   ← начинаем с первого элемента

  ┌──────┬──────────┬─────────────────────────────┬─────────────┐
  │  i   │ array[i] │ колбэк (acc + el)            │ accumulator │
  ├──────┼──────────┼─────────────────────────────┼─────────────┤
  │  0   │    1     │ callback(0, 1) = 0+1 = 1    │      1      │
  │  1   │    2     │ callback(1, 2) = 1+2 = 3    │      3      │
  │  2   │    3     │ callback(3, 3) = 3+3 = 6    │      6      │
  │  3   │    4     │ callback(6, 4) = 6+4 = 10   │     10      │
  │  4   │    5     │ callback(10,5) = 10+5 = 15  │     15      │
  │  5   │    —     │ 5 < 5 = false, стоп          │             │
  └──────┴──────────┴─────────────────────────────┴─────────────┘

  return 15

────────────────────────────────────────────

Пошагово для reduce([1, 2, 3], (acc, el) => acc + el):
  ← без initialValue!

  hasInitialValue = false
  accumulator = array[0] = 1  ← первый элемент стал аккумулятором
  startIndex = 1              ← начинаем со второго элемента

  ┌──────┬──────────┬──────────────────────────┬─────────────┐
  │  i   │ array[i] │ колбэк (acc + el)         │ accumulator │
  ├──────┼──────────┼──────────────────────────┼─────────────┤
  │  1   │    2     │ callback(1, 2) = 1+2 = 3 │      3      │
  │  2   │    3     │ callback(3, 3) = 3+3 = 6 │      6      │
  │  3   │    —     │ 3 < 3 = false, стоп       │             │
  └──────┴──────────┴──────────────────────────┴─────────────┘

  return 6
```

---

## Сравнение всех функций

| Функция | Возвращает | Останавливается раньше? | Зачем? |
|---------|-----------|------------------------|--------|
| `forEach` | `undefined` | ❌ Нет | Выполнить действие для каждого элемента |
| `map` | Новый массив (та же длина) | ❌ Нет | Преобразовать каждый элемент |
| `filter` | Новый массив (короче или равен) | ❌ Нет | Отобрать подходящие элементы |
| `find` | Элемент или `undefined` | ✅ На первом `true` | Найти первый подходящий |
| `some` | `boolean` | ✅ На первом `true` | Есть ли хоть один подходящий? |
| `every` | `boolean` | ✅ На первом `false` | Все ли подходят? |
| `reduce` | Любое значение | ❌ Нет | Накопить результат |

---

## Контрольные вопросы

### 1. В чём преимущества колбэков при работе с массивами?

Колбэки делают функции **универсальными**. Без колбэков для каждой задачи нужна отдельная функция — это показано в примере с `printArray` и `printArray1`. С колбэками одна функция `forEach` может и выводить в консоль, и считать сумму, и делать что угодно другое — логику задаёт тот, кто вызывает функцию.

### 2. Какие проблемы могут возникать при использовании колбэков?

- **Передача не-функции** — решается проверкой `typeof callback !== "function"` с выбросом `TypeError`
- **Передача не-массива** — решается проверкой `Array.isArray(array)`
- **"Ад колбэков"** (callback hell) — когда колбэки глубоко вложены друг в друга, код становится нечитаемым. Решается через `Promise` и `async/await`

### 3. Как реализовать функции без встроенных методов?

Через обычный цикл `for`, свойство `length` и метод `push`. Каждая функция принимает массив и колбэк, обходит массив циклом и вызывает `callback(array[i], i, array)` на каждом шаге. Отличия только в том, что делается с результатом колбэка:

```
forEach → результат колбэка игнорируется
map     → результат колбэка кладётся в result
filter  → если колбэк = true, оригинальный элемент кладётся в result
find    → если колбэк = true, возвращаем элемент и выходим
some    → если колбэк = true, возвращаем true и выходим
every   → если колбэк = false, возвращаем false и выходим
reduce  → результат колбэка становится новым аккумулятором
```
